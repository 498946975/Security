## 文件包含漏洞防御
```yaml
1.设置白名单(文件名可以确定)
2.过滤危险字符(判断文件名称是否为合法的php文件) 
3.设置文件目录(对可以包含的文件进行限制，可以使用白名单的方式，或者设置可以包含的目录) 
4.关闭危险配置(无需情况下设置allow_url_include和allow_url_fopen为关闭)
5.严格检查include类的文件包含函数中的参数是否外界可控。
```
